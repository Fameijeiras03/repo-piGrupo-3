<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Document</title>
</head>
<body>
	<img src= <%= user.fotoDePerfil %> alt="Foto del Auto">
	<h1>
		<%= user.nombre %>
	</h1>
	<h2>
		<%= user.email %>
	</h2>
	<h3>
		Productos
	</h3>
	




	<ul>
		<% for (let i = 0; i < listProducts.length; i++) { %>
			<li>
        <img src="<%= listProducts[i].imagen %>" alt="Foto del Producto" width="100">

        <ul>
			<li><strong>Nombre:</strong> <%= listProducts[i].nombre %></li>
			<li><strong>Descripción:</strong> <%= listProducts[i].descripcion %></li>
			<li>
				<strong>Cant. comentarios:</strong> 
					<% if (coments[i]) { %>
						<%= coments[i].length %>
					<% } else { %>
						0
					<% } %>
			</li>
			</ul>
				</li>
			<% } %>
	</ul>













</body>
</html>