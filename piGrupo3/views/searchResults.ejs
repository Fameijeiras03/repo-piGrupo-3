<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Document</title>
	<!-- Font Awesome Stylesheet -->
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
	<!-- Bootstrap Grid Stylesheet -->
	<link rel="stylesheet" href="/stylesheets/bootstrap-grid.min.css">
	<!-- Own Stylesheet -->
	<link rel="stylesheet" href="/stylesheets/app.css">
</head>
<body>
	<% if (locals.user != undefined) { %>
		<%- include('partials/headerLogueado.ejs') %>
	<% } else { %>
		<%- include('partials/header.ejs') %>
	<% } %>
	
		<!-- Resultados de búsqueda -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Resultados de búsqueda para: "<%= search %>"</h2>
			</div>
			
			<!-- Verificar si hay productos -->
			<% if (products.length == 0) { %>
				<div class="col-12">
					<p>No hay resultados para su criterio de búsqueda: "<%= search %>"</p>
				</div>
			<% } else { %>
				<!-- Mostrar TODOS los productos encontrados -->
				<% for (let i = 0; i < products.length; i++) { %>
					<div class="col-12 col-sm-6 col-lg-3">
						<section class="product-box">
							<a href="/product/<%= products[i].id %>">
								<figure class="product-box_image">
									<img src='<%= products[i].imagen %>' alt="" width="100">
								</figure>
								<article class="product-box_data">
									<h2><%= products[i].producto %></h2>
									<p><%= products[i].descripcion %></p>
									<!-- Nombre del usuario que cargó el producto -->
									<p>Por: <a href="/users/profile/<%= products[i].user.id %>"><%= products[i].user.nombreUsuario %></a></p>
								</article>
							</a>
						</section>
					</div>
				<% } %>
			<% } %>
		</div>
	</div>


	<%- include('partials/footer.ejs') %>

</body>

</html>